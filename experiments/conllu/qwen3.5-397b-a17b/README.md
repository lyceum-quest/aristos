# Qwen3.5 397B A17B CoNLL-U experiment — deprecated

This arm is retained only as experiment-1 provenance. It is excluded from Kai tasks and future runs because the pinned DeepInfra system repeatedly failed with HTTP 429, 499, and 504 responses; its sole returned output was malformed one-line CoNLL-U.

The original byte-identical pinned GLAUx XML inputs and captured `outputs/experiment_1/` artifact remain available for audit. Raw request and response records remain under the ignored local `responses/` directory.
